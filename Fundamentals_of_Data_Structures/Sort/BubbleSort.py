def BubbleSort(numbers, numbersSize):
   for i in range( numbersSize - 1):
      for j in range( numbersSize - i - 1):
         if (numbers[j] > numbers[j+1]) {
            temp = numbers[j]
            numbers[j] = numbers[j + 1]
            numbers[j + 1] = temp
         }
      }
   }
}